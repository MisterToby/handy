<?xml version="1.0" encoding="UTF-8"?>
<subsonic-response xmlns="http://subsonic.org/restapi" status="ok" version="1.9.0">
  <indexes lastModified="1423414508703">
      {% for key, array in index %}
        <index name="{{ key }}">
        {% for item in array %}
            <artist name="{{ item.muiName }}" id="{{ item.id }}"/>
        {% endfor %}
        </index>
      {% endfor %}
  </indexes>
</subsonic-response>

