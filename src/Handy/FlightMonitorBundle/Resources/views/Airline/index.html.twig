{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Airline list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Airname</th>
                <th>Airurl</th>
                <th>Airpost</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('airline_show', { 'id': entity.id }) }}">{{ entity.airName }}</a></td>
                <td>{{ entity.airUrl }}</td>
                <td>{{ entity.airPost }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('airline_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('airline_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('airline_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
