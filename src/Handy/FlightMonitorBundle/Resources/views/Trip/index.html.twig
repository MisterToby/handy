{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Trip list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Tridescription</th>
                <!-- <th>Trifields</th> -->
                <th>Triprocessingmethod</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('trip_show', { 'id': entity.id }) }}">{{ entity.triDescription }}</a></td>
                <!-- <td>{{ entity.triFields }}</td> -->
                <td>{{ entity.triProcessingMethod }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('trip_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('trip_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                    <li>
                        <a href="{{ path('trip_report', { 'id': entity.id }) }}">report</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('trip_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
